# Python
__pycache__/
*.pyc
venv/
.venv/

# Env
.env

# OS
.DS_Store

# NOTE: model/*.keras and model/tokenizer.pkl are intentionally NOT
# ignored - they're small (~4MB total) and the app is broken without
# them. Commit them, or if they grow too large later, host them
# externally and download in a setup step instead of silently excluding
# them from git.
